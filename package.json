{
  "name": "reboot-ubiquiti-nano-beam",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node .",
    "build": "npm run build-js && npm run build-exe",
    "build-js": "rimraf ./build && tsc",
    "build-exe": "nexe --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "puppeteer": "^20.3.0"
  },
  "devDependencies": {
    "nexe": "^4.0.0-rc.2",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
